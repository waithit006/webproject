import { LikebuttonDirective } from './likebutton.directive';

describe('LikebuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new LikebuttonDirective();
    expect(directive).toBeTruthy();
  });
});
